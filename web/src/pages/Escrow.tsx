import EscrowCard from "../components/EscrowCard";

export default function Escrow() {
  return (
    <div className="flex items-center justify-center p-20 h-screen w-full">
      <EscrowCard />
    </div>
  );
}
